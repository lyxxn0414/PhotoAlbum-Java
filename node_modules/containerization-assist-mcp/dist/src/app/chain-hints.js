/**
 * Chain Hints Registry
 * Central configuration for tool workflow guidance at application level
 */
import { TOOL_NAME } from '../tools/index.js';
/**
 * Default chain hints for all containerization tools
 * Provides workflow guidance for successful tool chaining
 *
 * Uses TOOL_NAME enum to prevent drift from hardcoded strings
 */
export const DEFAULT_CHAIN_HINTS = {
    [TOOL_NAME.ANALYZE_REPO]: {
        success: 'Repository analysis completed successfully. Continue by calling the generate-dockerfile or fix-dockerfile tools to create or fix your Dockerfile.',
        failure: 'Repository analysis failed. Please check the logs for details.',
    },
    [TOOL_NAME.BUILD_IMAGE_CONTEXT]: {
        success: 'Build context prepared. Next: Execute the provided build command, then call scan-image to check for security vulnerabilities.',
        failure: 'Build context preparation failed. Use fix-dockerfile to resolve issues, then retry build-image-context.',
    },
    [TOOL_NAME.FIX_DOCKERFILE]: {
        success: 'Dockerfile fixes applied successfully. Next: Call build-image-context to test the fixed Dockerfile.',
        failure: 'Dockerfile fix failed. Review validation errors and try manual fixes.',
    },
    [TOOL_NAME.GENERATE_K8S_MANIFESTS]: {
        success: 'Kubernetes manifests generated successfully. Next: Call prepare-cluster to create a kind cluster to deploy to.',
        failure: 'Manifest generation failed. Ensure you have a valid image and try again.',
    },
    [TOOL_NAME.PREPARE_CLUSTER]: {
        success: 'Cluster preparation successful. Next: Use `kubectl apply -f <manifest-folder>` to deploy your manifests to the cluster, then call verify-deploy to check deployment status.',
        failure: 'Cluster preparation found issues. Check connectivity, permissions, and namespace configuration.',
    },
    [TOOL_NAME.PUSH_IMAGE]: {
        success: 'Image pushed successfully. Review AI optimization insights for push improvements.',
        failure: 'Image push failed. Check registry credentials, network connectivity, and image tag format.',
    },
    [TOOL_NAME.SCAN_IMAGE]: {
        success: 'Security scan passed! Proceed with push-image to push to a registry, or continue with deployment preparation.',
        failure: 'Security vulnerabilities detected. Review scan results and apply fixes before proceeding.',
    },
};
//# sourceMappingURL=chain-hints.js.map