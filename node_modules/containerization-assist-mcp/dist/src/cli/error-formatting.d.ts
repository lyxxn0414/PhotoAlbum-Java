/**
 * Centralized error formatting for CLI commands
 * Ensures consistent error messages and exit behavior
 */
import type { Result } from '../types/core.js';
/**
 * Handle Result errors consistently across CLI commands
 */
export declare function handleResultError<T>(result: Result<T>, message: string): never;
/**
 * Handle generic errors consistently across CLI commands
 */
export declare function handleGenericError(message: string, error?: unknown): never;
//# sourceMappingURL=error-formatting.d.ts.map