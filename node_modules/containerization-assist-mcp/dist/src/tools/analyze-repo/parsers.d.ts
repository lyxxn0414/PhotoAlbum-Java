/**
 * Deterministic parsers for repository configuration files
 *
 * Each parser reads a project configuration file and extracts:
 * - Language and version
 * - Framework and version
 * - Dependencies
 * - Build system
 * - Default ports
 * - Entry points
 */
export interface ParsedConfig {
    language?: 'java' | 'dotnet' | 'javascript' | 'typescript' | 'python' | 'rust' | 'go' | 'other';
    framework?: string;
    frameworkVersion?: string;
    languageVersion?: string;
    dependencies?: string[];
    ports?: number[];
    entryPoint?: string;
    buildSystem?: {
        type: string;
        version?: string;
    };
}
/**
 * Parse package.json for Node.js projects
 */
export declare function parsePackageJson(filePath: string): Promise<ParsedConfig>;
/**
 * Parse pom.xml for Java/Maven projects
 */
export declare function parsePomXml(filePath: string): Promise<ParsedConfig>;
/**
 * Parse build.gradle(.kts) for Java/Gradle projects
 */
export declare function parseGradle(filePath: string): Promise<ParsedConfig>;
/**
 * Parse requirements.txt / pyproject.toml for Python projects
 */
export declare function parsePythonConfig(filePath: string): Promise<ParsedConfig>;
/**
 * Parse Cargo.toml for Rust projects
 */
export declare function parseCargoToml(filePath: string): Promise<ParsedConfig>;
/**
 * Parse .csproj for .NET projects
 */
export declare function parseCsProj(filePath: string): Promise<ParsedConfig>;
/**
 * Parse go.mod for Go projects
 */
export declare function parseGoMod(filePath: string): Promise<ParsedConfig>;
//# sourceMappingURL=parsers.d.ts.map