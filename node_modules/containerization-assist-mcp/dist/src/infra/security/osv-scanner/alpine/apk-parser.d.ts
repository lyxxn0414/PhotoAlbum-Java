import type { Logger } from 'pino';
import type { ExtractedPackage } from '../maven/types.js';
/**
 * Parses Alpine APK package database format from /lib/apk/db/installed
 *
 * Format:
 * - Each package is a block of key:value lines
 * - Packages are separated by blank lines
 * - Key fields:
 *   P: - Package name
 *   V: - Version
 *   A: - Architecture
 *   o: - Origin package name
 *   c: - Commit/checksum
 *
 * Example:
 * P:musl
 * V:1.2.3-r0
 * A:x86_64
 *
 * P:busybox
 * V:1.35.0-r17
 * A:x86_64
 *
 * @param release - Alpine release version (e.g., "3.18" for Alpine 3.18)
 */
export declare function parseAlpinePackages(tarBuffer: Buffer, filePath: string, logger: Logger, release?: string): Promise<ExtractedPackage[]>;
//# sourceMappingURL=apk-parser.d.ts.map