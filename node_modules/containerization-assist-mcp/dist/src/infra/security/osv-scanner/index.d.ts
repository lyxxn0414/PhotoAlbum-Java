import type { Logger } from 'pino';
import type Dockerode from 'dockerode';
import { Result } from '../../../types/index.js';
import type { BasicScanResult } from '../scanner.js';
export declare function checkOSVAvailability(logger: Logger): Promise<Result<string>>;
export declare function scanImageWithOSV(docker: Dockerode, imageId: string, logger: Logger): Promise<Result<BasicScanResult>>;
//# sourceMappingURL=index.d.ts.map