"use strict";
/**
 * Health Check Module
 * Consolidated health check logic for Docker and Kubernetes dependencies
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkDockerHealth = checkDockerHealth;
exports.checkKubernetesHealth = checkKubernetesHealth;
const dockerode_1 = __importDefault(require("dockerode"));
const socket_validation_1 = require("../docker/socket-validation");
const client_1 = require("../kubernetes/client");
const errors_1 = require("../../lib/errors");
/**
 * Default timeout for health checks in milliseconds
 */
const DEFAULT_TIMEOUT_MS = 3000;
/**
 * Check Docker daemon health and connectivity
 *
 * @param logger - Logger instance for diagnostic output
 * @param options - Optional configuration
 * @returns Docker availability status with version or error details
 */
async function checkDockerHealth(logger, options = {}) {
    const timeout = options.timeout ?? DEFAULT_TIMEOUT_MS;
    try {
        const socketPath = (0, socket_validation_1.autoDetectDockerSocket)();
        const docker = new dockerode_1.default({ socketPath });
        const versionInfo = await Promise.race([
            docker.version(),
            new Promise((_, reject) => setTimeout(() => reject(new Error('Docker connection timeout')), timeout)),
        ]);
        return {
            available: true,
            version: versionInfo.Version,
        };
    }
    catch (error) {
        logger.debug({ error }, 'Docker health check failed');
        return {
            available: false,
            error: (0, errors_1.extractErrorMessage)(error),
        };
    }
}
/**
 * Check Kubernetes cluster health and connectivity
 *
 * @param logger - Logger instance for diagnostic output
 * @param options - Optional configuration
 * @returns Kubernetes availability status with version or error details
 */
async function checkKubernetesHealth(logger, options = {}) {
    const timeout = options.timeout ?? DEFAULT_TIMEOUT_MS;
    try {
        const k8sClient = (0, client_1.createKubernetesClient)(logger);
        const connected = await Promise.race([
            k8sClient.ping(),
            new Promise((_, reject) => setTimeout(() => reject(new Error('Kubernetes connection timeout')), timeout)),
        ]);
        if (connected) {
            return {
                available: true,
                version: 'connected',
            };
        }
        return {
            available: false,
            error: 'Unable to connect to Kubernetes cluster',
        };
    }
    catch (error) {
        logger.debug({ error }, 'Kubernetes health check failed');
        return {
            available: false,
            error: (0, errors_1.extractErrorMessage)(error),
        };
    }
}
//# sourceMappingURL=checks.js.map