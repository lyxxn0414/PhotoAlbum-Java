/**
 * Property Resolution
 * Local property resolution for Maven POM files (no remote fetching).
 */
import type { Logger } from 'pino';
import type { PomProject } from './types';
/** Extract local properties from current POM only (no parent fetching) */
export declare function extractLocalProperties(project: PomProject, logger: Logger): Map<string, string>;
/** Extract local dependency management from current POM only (no parent fetching) */
export declare function extractLocalDependencyManagement(project: PomProject): Map<string, string>;
/** Resolve property placeholders like ${project.version} or ${spring.version} */
export declare function resolvePropertyValue(value: string, properties: Map<string, string>, project: PomProject): string;
//# sourceMappingURL=property-resolver.d.ts.map