import type { Logger } from 'pino';
import { Result } from '../../types';
interface SecurityScanner {
    scanImage: (imageId: string) => Promise<Result<BasicScanResult>>;
    ping: () => Promise<Result<boolean>>;
}
export interface BasicScanResult {
    imageId: string;
    vulnerabilities: Array<{
        id: string;
        severity: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL' | 'NEGLIGIBLE' | 'UNKNOWN';
        package: string;
        version: string;
        fixedVersion?: string;
        description: string;
    }>;
    totalVulnerabilities: number;
    criticalCount: number;
    highCount: number;
    mediumCount: number;
    lowCount: number;
    negligibleCount: number;
    unknownCount: number;
    scanDate: Date;
}
/**
 * Create a security scanner based on the specified type
 *
 * @param logger - Logger instance
 * @param scannerType - Type of scanner to create ('trivy', 'stub', or undefined for 'trivy')
 * @returns SecurityScanner instance
 */
export declare const createSecurityScanner: (logger: Logger, scannerType?: string) => SecurityScanner;
export {};
//# sourceMappingURL=scanner.d.ts.map