"use strict";
/**
 * Zod schema for the aks-loop prompt arguments.
 *
 * MCP prompt arguments are always strings at the protocol level.
 * Optional fields default to sensible values in the prompt builder;
 * required fields (registry, resourceGroup, clusterName) must be provided.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.aksRemoteDevLoopSchema = void 0;
const zod_1 = require("zod");
const schema_1 = require("../shared/schema");
exports.aksRemoteDevLoopSchema = {
    ...schema_1.sharedLoopSchema,
    registry: zod_1.z
        .string()
        .describe('Container registry URL (e.g., myregistry.azurecr.io). Required for pushing images to ACR'),
    resourceGroup: zod_1.z.string().describe('Azure resource group containing the AKS cluster'),
    clusterName: zod_1.z.string().describe('AKS cluster name to deploy to'),
};
//# sourceMappingURL=schema.js.map