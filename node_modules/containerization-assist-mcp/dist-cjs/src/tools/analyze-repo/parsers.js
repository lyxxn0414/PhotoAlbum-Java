"use strict";
/**
 * Deterministic parsers for repository configuration files
 *
 * Each parser reads a project configuration file and extracts:
 * - Language and version
 * - Framework and version
 * - Dependencies
 * - Build system
 * - Default ports
 * - Entry points
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.parsePackageJson = parsePackageJson;
exports.parsePomXml = parsePomXml;
exports.parseGradle = parseGradle;
exports.parsePythonConfig = parsePythonConfig;
exports.parseCargoToml = parseCargoToml;
exports.parseCsProj = parseCsProj;
exports.parseGoMod = parseGoMod;
const node_fs_1 = require("node:fs");
const toml = __importStar(require("@iarna/toml"));
const xml2js_1 = require("xml2js");
const constants_1 = require("../../config/constants");
/**
 * Parse package.json for Node.js projects
 */
async function parsePackageJson(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const pkg = JSON.parse(content);
        let framework;
        const deps = { ...pkg.dependencies, ...pkg.devDependencies };
        if (deps['express'])
            framework = 'express';
        else if (deps['@nestjs/core'])
            framework = 'nestjs';
        else if (deps['next'])
            framework = 'next';
        else if (deps['nuxt'])
            framework = 'nuxt';
        else if (deps['react'])
            framework = 'react';
        else if (deps['vue'])
            framework = 'vue';
        else if (deps['@angular/core'])
            framework = 'angular';
        const ports = [];
        if (pkg.scripts) {
            // Look for PORT= or --port patterns
            const scriptStr = JSON.stringify(pkg.scripts);
            const portMatches = scriptStr.match(/(?:PORT=|--port[=\s]+)(\d+)/g);
            if (portMatches) {
                portMatches.forEach((match) => {
                    const portMatch = match.match(/\d+/);
                    if (portMatch) {
                        const port = parseInt(portMatch[0], 10);
                        if (port > 0 && port < 65536)
                            ports.push(port);
                    }
                });
            }
        }
        if (ports.length === 0) {
            if (framework === 'next' || framework === 'react')
                ports.push(constants_1.FRAMEWORK_PORTS.next);
            else if (framework === 'angular')
                ports.push(constants_1.FRAMEWORK_PORTS.angular);
            else if (framework === 'nuxt' || framework === 'vue')
                ports.push(constants_1.FRAMEWORK_PORTS.nuxt);
            else
                ports.push(constants_1.FRAMEWORK_PORTS.next); // Default Node.js port
        }
        let entryPoint = pkg.main || 'index.js';
        if (pkg.scripts?.start) {
            const startScript = pkg.scripts.start;
            // Extract entry point from "node server.js" or "ts-node src/index.ts"
            const match = startScript.match(/(?:node|ts-node)\s+(.+?)(?:\s|$)/);
            if (match)
                entryPoint = match[1];
        }
        const buildSystem = {
            type: 'npm',
            version: pkg.engines?.node,
        };
        const isTypeScript = !!(deps['typescript'] ||
            deps['ts-node'] ||
            deps['@typescript-eslint/parser']);
        const result = {
            language: isTypeScript ? 'typescript' : 'javascript',
            dependencies: Object.keys(deps).slice(0, 20), // Top 20 deps
            ports,
            entryPoint,
            buildSystem,
        };
        if (framework)
            result.framework = framework;
        if (pkg.engines?.node)
            result.languageVersion = pkg.engines.node;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse package.json: ${error}`);
    }
}
/**
 * Parse pom.xml for Java/Maven projects
 */
async function parsePomXml(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const pom = await (0, xml2js_1.parseStringPromise)(content);
        const project = pom.project;
        const artifactId = project.artifactId?.[0] || '';
        const version = project.version?.[0] || '';
        let framework;
        const dependencies = project.dependencies?.[0]?.dependency || [];
        for (const dep of dependencies) {
            const depGroupId = dep.groupId?.[0] || '';
            const depArtifactId = dep.artifactId?.[0] || '';
            if (depGroupId.includes('springframework.boot')) {
                framework = 'spring-boot';
                break;
            }
            else if (depArtifactId.includes('quarkus')) {
                framework = 'quarkus';
                break;
            }
            else if (depArtifactId.includes('micronaut')) {
                framework = 'micronaut';
                break;
            }
            else if (depGroupId.includes('jakarta.ee')) {
                framework = 'jakarta-ee';
                break;
            }
        }
        const properties = project.properties?.[0] || {};
        const javaVersion = properties['java.version']?.[0] ||
            properties['maven.compiler.source']?.[0] ||
            properties['maven.compiler.target']?.[0];
        const ports = [];
        if (framework === 'spring-boot')
            ports.push(8080);
        else if (framework === 'quarkus')
            ports.push(8080);
        else if (framework === 'micronaut')
            ports.push(8080);
        else
            ports.push(8080); // Default Java port
        const entryPoint = `${artifactId}-${version}.jar`;
        const result = {
            language: 'java',
            dependencies: dependencies
                .map((d) => {
                const dep = d;
                return `${dep.groupId?.[0] || ''}:${dep.artifactId?.[0] || ''}`;
            })
                .slice(0, 20),
            ports,
            entryPoint,
            buildSystem: {
                type: 'maven',
            },
        };
        if (framework)
            result.framework = framework;
        if (javaVersion)
            result.languageVersion = javaVersion;
        if (project.modelVersion?.[0] && result.buildSystem) {
            result.buildSystem.version = project.modelVersion[0];
        }
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse pom.xml: ${error}`);
    }
}
/**
 * Parse build.gradle(.kts) for Java/Gradle projects
 */
async function parseGradle(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        let framework;
        if (content.includes('org.springframework.boot'))
            framework = 'spring-boot';
        else if (content.includes('io.quarkus'))
            framework = 'quarkus';
        else if (content.includes('io.micronaut'))
            framework = 'micronaut';
        let javaVersion;
        const toolchainMatch = content.match(/languageVersion\s*=\s*JavaLanguageVersion\.of\((\d+)\)/);
        if (toolchainMatch) {
            javaVersion = toolchainMatch[1];
        }
        if (!javaVersion) {
            const sourceCompatMatch = content.match(/sourceCompatibility\s*=\s*['"]?(\d+)['"]?/);
            if (sourceCompatMatch)
                javaVersion = sourceCompatMatch[1];
        }
        if (!javaVersion) {
            const targetCompatMatch = content.match(/targetCompatibility\s*=\s*['"]?(\d+)['"]?/);
            if (targetCompatMatch)
                javaVersion = targetCompatMatch[1];
        }
        // Extract dependencies (limited to first 20)
        const dependencies = [];
        const depPattern = /implementation\s+['"]([^'"]+)['"]/g;
        let match;
        while ((match = depPattern.exec(content)) !== null) {
            if (match[1])
                dependencies.push(match[1]);
            if (dependencies.length >= 20)
                break;
        }
        const result = {
            language: 'java',
            dependencies,
            ports: framework === 'spring-boot' ? [8080] : [8080],
            buildSystem: {
                type: 'gradle',
            },
        };
        if (framework)
            result.framework = framework;
        if (javaVersion)
            result.languageVersion = javaVersion;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse build.gradle: ${error}`);
    }
}
/**
 * Parse requirements.txt / pyproject.toml for Python projects
 */
async function parsePythonConfig(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const isPyProject = filePath.endsWith('pyproject.toml');
        let framework;
        let dependencies = [];
        let pythonVersion;
        if (isPyProject) {
            // Parse TOML
            const parsed = toml.parse(content);
            const project = parsed.project || {};
            dependencies = project.dependencies || [];
            pythonVersion = project.requires_python;
            const depStr = dependencies.join(' ').toLowerCase();
            if (depStr.includes('django'))
                framework = 'django';
            else if (depStr.includes('flask'))
                framework = 'flask';
            else if (depStr.includes('fastapi'))
                framework = 'fastapi';
            else if (depStr.includes('tornado'))
                framework = 'tornado';
        }
        else {
            // Parse requirements.txt (simple line-by-line)
            dependencies = content
                .split('\n')
                .map((line) => line.trim())
                .filter((line) => line && !line.startsWith('#'))
                .slice(0, 20);
            const depStr = dependencies.join(' ').toLowerCase();
            if (depStr.includes('django'))
                framework = 'django';
            else if (depStr.includes('flask'))
                framework = 'flask';
            else if (depStr.includes('fastapi'))
                framework = 'fastapi';
        }
        const ports = [];
        if (framework === 'django')
            ports.push(constants_1.FRAMEWORK_PORTS.django);
        else if (framework === 'flask')
            ports.push(constants_1.FRAMEWORK_PORTS.flask);
        else if (framework === 'fastapi')
            ports.push(constants_1.FRAMEWORK_PORTS.fastapi);
        else
            ports.push(constants_1.FRAMEWORK_PORTS.django);
        const result = {
            language: 'python',
            dependencies: dependencies.slice(0, 20),
            ports,
            buildSystem: {
                type: isPyProject ? 'poetry' : 'pip',
            },
        };
        if (framework)
            result.framework = framework;
        if (pythonVersion)
            result.languageVersion = pythonVersion;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse Python config: ${error}`);
    }
}
/**
 * Parse Cargo.toml for Rust projects
 */
async function parseCargoToml(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const parsed = toml.parse(content);
        const package_ = parsed.package || {};
        const dependencies = Object.keys(parsed.dependencies || {});
        let framework;
        if (dependencies.includes('actix-web'))
            framework = 'actix-web';
        else if (dependencies.includes('rocket'))
            framework = 'rocket';
        else if (dependencies.includes('warp'))
            framework = 'warp';
        else if (dependencies.includes('axum'))
            framework = 'axum';
        const rustVersion = package_['rust-version'] || package_.edition;
        const ports = framework ? [8080] : [];
        const result = {
            language: 'rust',
            dependencies: dependencies.slice(0, 20),
            ports,
            buildSystem: {
                type: 'cargo',
            },
        };
        if (framework)
            result.framework = framework;
        if (rustVersion)
            result.languageVersion = rustVersion;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse Cargo.toml: ${error}`);
    }
}
/**
 * Parse .csproj for .NET projects
 */
async function parseCsProj(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const csproj = await (0, xml2js_1.parseStringPromise)(content);
        const project = csproj.Project;
        const propertyGroup = project.PropertyGroup?.[0] || {};
        const itemGroups = project.ItemGroup || [];
        const targetFramework = propertyGroup.TargetFramework?.[0];
        const dependencies = [];
        for (const itemGroup of itemGroups) {
            const packageRefs = itemGroup.PackageReference || [];
            for (const ref of packageRefs) {
                const name = ref.$?.Include;
                if (name)
                    dependencies.push(name);
            }
        }
        let framework;
        const depStr = dependencies.join(' ');
        if (depStr.includes('Microsoft.AspNetCore'))
            framework = 'aspnet-core';
        else if (depStr.includes('Microsoft.EntityFrameworkCore'))
            framework = 'entity-framework';
        const ports = framework === 'aspnet-core'
            ? [constants_1.FRAMEWORK_PORTS['aspnet-core'], constants_1.FRAMEWORK_PORTS['aspnet-core-https']]
            : [constants_1.FRAMEWORK_PORTS['aspnet-core']];
        const result = {
            language: 'dotnet',
            dependencies: dependencies.slice(0, 20),
            ports,
            buildSystem: {
                type: 'dotnet',
            },
        };
        if (framework)
            result.framework = framework;
        if (targetFramework)
            result.languageVersion = targetFramework;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse .csproj: ${error}`);
    }
}
/**
 * Parse go.mod for Go projects
 */
async function parseGoMod(filePath) {
    try {
        const content = await node_fs_1.promises.readFile(filePath, 'utf-8');
        const goVersionMatch = content.match(/^go\s+(\d+\.\d+)/m);
        const goVersion = goVersionMatch?.[1];
        const dependencies = [];
        const requireMatches = content.match(/require\s+\(([^)]+)\)/s);
        if (requireMatches?.[1]) {
            const requireBlock = requireMatches[1];
            const lines = requireBlock.split('\n');
            for (const line of lines) {
                const match = line.trim().match(/^([^\s]+)/);
                if (match?.[1])
                    dependencies.push(match[1]);
                if (dependencies.length >= 20)
                    break;
            }
        }
        let framework;
        const depStr = dependencies.join(' ');
        if (depStr.includes('gin-gonic/gin'))
            framework = 'gin';
        else if (depStr.includes('labstack/echo'))
            framework = 'echo';
        else if (depStr.includes('gofiber/fiber'))
            framework = 'fiber';
        else if (depStr.includes('gorilla/mux'))
            framework = 'gorilla';
        const result = {
            language: 'go',
            dependencies,
            ports: [8080],
            buildSystem: {
                type: 'go',
            },
        };
        if (framework)
            result.framework = framework;
        if (goVersion)
            result.languageVersion = goVersion;
        return result;
    }
    catch (error) {
        throw new Error(`Failed to parse go.mod: ${error}`);
    }
}
//# sourceMappingURL=parsers.js.map