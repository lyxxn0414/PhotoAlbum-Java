"use strict";
/**
 * Push image tool parameter validation schemas.
 * Defines the structure and validation rules for push operations.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.pushImageSchema = void 0;
const zod_1 = require("zod");
const schemas_1 = require("../shared/schemas");
exports.pushImageSchema = zod_1.z.object({
    imageId: zod_1.z.string().min(1).describe('Docker image ID or name to push'),
    registry: zod_1.z.string().min(1).describe('Target registry hostname (e.g., myregistry.azurecr.io, docker.io)'),
    platform: schemas_1.platform,
    credentials: zod_1.z
        .object({
        username: zod_1.z.string(),
        password: zod_1.z.string(),
    })
        .optional()
        .describe('Registry credentials. If not provided, will attempt to use Docker credential helpers'),
});
//# sourceMappingURL=schema.js.map