import { CustomerChannel } from "./common/models";
import { IToolStateManager } from "./common/toolStateManager";
import { AzureClient } from "./toolsImplementations/shared/azureClient";
import { ITelemetryClient } from "./toolsImplementations/shared/telemetryClient";
import { ToolType } from "./toolsImplementations/shared/toolType";
export interface McpStateManagerOptions {
    disableTelemetry: boolean;
    getCustomerChannel: () => CustomerChannel;
    isVsCode: boolean;
    version: string;
}
export declare class McpToolStateManager implements IToolStateManager {
    private isForVsCode;
    private workspaceIdMap;
    private registeredTools;
    private getCustomerChannel;
    private telemetryClient;
    private version;
    constructor(options: McpStateManagerOptions);
    toolTypeExists(toolType: ToolType): boolean;
    getOrCreateWorkspaceId(workspaceFolder: string | undefined): string;
    registerTool(toolType: ToolType, toolName: string): void;
    getToolName(toolType: ToolType): string;
    getTelemetryClient(): ITelemetryClient;
    getIsForVsCode(): boolean;
    getCopilotMarkdownExtension(): string;
    getAzureClient(subscriptionId: string): AzureClient;
    getCustomerChannelValue(): CustomerChannel;
    getVersion(): string;
}
