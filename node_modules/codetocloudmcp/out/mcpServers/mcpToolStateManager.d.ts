import { AbstractToolStateManager, AbstractToolStateManagerOptions } from "../common/abstractToolStateManager";
import { RequestHandlerExtra } from "@modelcontextprotocol/sdk/shared/protocol.js";
import { CallToolResult, ServerNotification, ServerRequest } from "@modelcontextprotocol/sdk/types.js";
import type { ToolHandlerOptions } from "containerization-assist-mcp";
import type { ToolName } from "containerization-assist-mcp/tools";
export interface McpStateManagerOptions extends AbstractToolStateManagerOptions {
    isVsCode: boolean;
    disableTelemetry?: boolean;
}
export declare class McpToolStateManager extends AbstractToolStateManager {
    private isForVsCode;
    private _containerAssistAppRuntimeConfig?;
    constructor(options: McpStateManagerOptions);
    getIsForVsCode(): boolean;
    getCopilotMarkdownExtension(): string;
    private getContainerAssistAppRuntime;
    handleContainerAssistToolCall(params: Record<string, unknown> | undefined, extra: RequestHandlerExtra<ServerRequest, ServerNotification>, toolName: ToolName, telemetryOptions: ToolHandlerOptions): Promise<CallToolResult>;
}
