import { ITelemetryClient } from "../../toolsImplementations/shared/telemetryClient";
import { CustomerChannel } from "../models";
import { ResultCode, TelemetryProperties } from "./telemetryUtil";
export declare class TelemetryClient implements ITelemetryClient {
    private client;
    private getCustomerChannel;
    private sessionId;
    private correlationId;
    private disableTelemetry;
    constructor(getCustomerChannel: () => CustomerChannel, disableTelemetry: boolean);
    sendEvent(eventName: string, properties: TelemetryProperties): void;
    initTelemetryProperties(version: string, toolName: string, projectId: string, resultCode?: ResultCode): Promise<TelemetryProperties>;
}
