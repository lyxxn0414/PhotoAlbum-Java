export declare enum PlanStepBulletType {
    Numbered = 0,
    Bulleted = 1
}
export declare class PlanStep {
    private bulletType;
    private stepName;
    private children;
    constructor(stepName: string, bulletType: PlanStepBulletType);
    addStep(planStep: PlanStep | string): PlanStep;
    addSteps(planSteps: (PlanStep | string)[]): void;
    render(): string[];
    renderIntoMarkdown(): string;
}
