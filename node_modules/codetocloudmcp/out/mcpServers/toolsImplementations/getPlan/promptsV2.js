"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.populateGetPlanPrompts = populateGetPlanPrompts;
const models_1 = require("../../common/models");
const generateMermaid_1 = require("../getAppTopology/generateMermaid");
const commonPrompts_1 = require("./commonPrompts");
const azCliExecutionSteps_1 = require("./executionSteps/azCliExecutionSteps");
const azdExecutionSteps_1 = require("./executionSteps/azdExecutionSteps");
function populateGetPlanPrompts(param, stateManager, targetAppServices) {
    if (param.deployOption === models_1.DeployOption.deployOnly) {
        param.provisioningTool = models_1.DeploymentTool.AzCLI;
        param.sourceType = models_1.SourceType.fromAzure;
    }
    if (param.deployOption === models_1.DeployOption.provisionOnly) {
        param.provisioningTool = models_1.DeploymentTool.AzCLI;
    }
    let stepProvider;
    if (param.provisioningTool === models_1.DeploymentTool.AzCLI) {
        stepProvider = new azCliExecutionSteps_1.AzCliExecutionStepGenerator(param, stateManager);
    }
    else {
        stepProvider = new azdExecutionSteps_1.AzdExecutionStepGenerator(param, stateManager);
    }
    const executionSteps = stepProvider.generateExecutionSteps();
    var goal = param.sourceType === models_1.SourceType.fromAzure ?
        `Based on the project to provide a plan to deploy the project to Azure ${targetAppServices.join(", ")} in resource group ${param.resourceGroupName || ''} and subscription ${param.subscriptionId || ''} with tool ${param.provisioningTool.toLocaleUpperCase()}`
        : `Based on the project to provide a plan to deploy the project to Azure using ${param.provisioningTool.toLocaleUpperCase()}. `;
    if (param.deployOption === models_1.DeployOption.provisionOnly) {
        goal = `Provide a plan to provision Azure resources for the project with ${param.provisioningTool.toLocaleUpperCase()}${param.iacOptions ? ' and ' + param.iacOptions : ''}.`;
    }
    const mermaidDiagram = (0, generateMermaid_1.generateMermaidChart)(param.workspaceFolder, param.services);
    return (0, commonPrompts_1.finalizePlan)(param.projectName, targetAppServices, param.sourceType, goal, executionSteps, mermaidDiagram, stateManager, param.deployOption);
}
