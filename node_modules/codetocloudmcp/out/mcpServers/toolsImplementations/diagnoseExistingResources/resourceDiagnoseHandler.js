"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResourceDiagnoseHandler = void 0;
class ResourceDiagnoseHandler {
    stateManager;
    constructor(params, stateManager) {
        this.stateManager = stateManager;
    }
    formatDiagnosticResults(results) {
        const formatted = [];
        for (const result of results) {
            if (result.success) {
                formatted.push(`✅ ${result.message}`);
            }
            else {
                formatted.push(`❌ ${result.message}`);
                if (result.recommendations && result.recommendations.length > 0) {
                    formatted.push('\n💡 Recommendations:');
                    for (const recommendation of result.recommendations) {
                        formatted.push(`   • ${recommendation}`);
                    }
                }
            }
            formatted.push(''); // Add spacing between results
        }
        return formatted.join('\n');
    }
}
exports.ResourceDiagnoseHandler = ResourceDiagnoseHandler;
