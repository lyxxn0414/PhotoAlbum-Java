/**
 * IPv4 IP/CIDR helpers for ACA Key Vault firewall rule matching (e.g. Key Vault IP rules).
 */
/** Check if an IP is allowed by at least one rule (exact IP or CIDR). */
export declare function ipMatchesAnyRule(ip: string, ruleValues: string[]): boolean;
export declare function ipMatchesRule(ip: string, ruleValue: string): boolean;
/** Simple IPv4 CIDR check (e.g. "10.0.0.0/24"). */
export declare function ipInCidr(ip: string, cidr: string): boolean;
export declare function ipToNumber(ip: string): number | null;
