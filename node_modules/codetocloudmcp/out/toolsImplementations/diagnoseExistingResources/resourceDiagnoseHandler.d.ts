import { IToolStateManager } from "../../common/toolStateManager";
import { DiagnoseExistingResourcesParameters } from "./params";
export interface DiagnoseResult {
    success: boolean;
    message: string;
    recommendations?: string[];
}
export declare abstract class ResourceDiagnoseHandler {
    protected stateManager: IToolStateManager;
    constructor(params: DiagnoseExistingResourcesParameters, stateManager: IToolStateManager);
    abstract diagnose(): Promise<string>;
    protected formatDiagnosticResults(results: DiagnoseResult[]): string;
}
