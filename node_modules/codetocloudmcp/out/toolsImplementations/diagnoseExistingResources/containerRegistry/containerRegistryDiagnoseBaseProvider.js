"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ContainerRegistryDiagnoseProvider = void 0;
const resourceDiagnoseHandler_1 = require("../resourceDiagnoseHandler");
const containerRegistryToAcaDiagnosticHandler_1 = require("./containerRegistryToAcaDiagnosticHandler");
const containerRegistryToAksDiagnosticHandler_1 = require("./containerRegistryToAksDiagnosticHandler");
class ContainerRegistryDiagnoseProvider extends resourceDiagnoseHandler_1.ResourceDiagnoseHandler {
    containerRegistryId;
    containerAppIds;
    kubernetesIds;
    constructor(params, stateManager) {
        super(params, stateManager);
        this.containerRegistryId = params.containerRegistryConnections?.containerRegistryId ?? '';
        this.containerAppIds = params.containerRegistryConnections ? [...new Set(params.containerRegistryConnections.containerAppIds)] : [];
        this.kubernetesIds = params.containerRegistryConnections ? [...new Set(params.containerRegistryConnections.kubernetesIds)] : [];
    }
    async diagnose() {
        const headerLines = [];
        // Parse container registry ID
        const registryRegex = /^\/subscriptions\/([^\/]+)\/resourceGroups\/([^\/]+)\/providers\/Microsoft\.ContainerRegistry\/registries\/([^\/]+)$/i;
        const registryMatch = this.containerRegistryId.match(registryRegex);
        if (!registryMatch) {
            return `Invalid containerRegistryId format: ${this.containerRegistryId}. Expected format: /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ContainerRegistry/registries/{registryName}`;
        }
        headerLines.push(`\n🔍 Diagnosing Container Registry connections:`);
        headerLines.push(`Registry: ${this.containerRegistryId}`);
        const [, registrySubscriptionId, registryResourceGroupName, registryName] = registryMatch;
        // Get registry information
        let registry;
        try {
            const azureClient = await this.stateManager.getAzureClient(registrySubscriptionId);
            registry = await azureClient.getContainerRegistryDetails(registryResourceGroupName, registryName);
        }
        catch (error) {
            return `Failed to retrieve container registry details for ${this.containerRegistryId}. Error: ${error.message}`;
        }
        const acaPromises = this.containerAppIds.map(containerAppId => new containerRegistryToAcaDiagnosticHandler_1.ContainerRegistryToAcaDiagnosticHandler(this.stateManager).diagnoseResource(containerAppId, registry));
        const aksPromises = this.kubernetesIds.map(kubernetesId => new containerRegistryToAksDiagnosticHandler_1.ContainerRegistryToAksDiagnosticHandler(this.stateManager).diagnoseResource(kubernetesId, registry));
        const result = await Promise.all([...acaPromises, ...aksPromises]);
        headerLines.push(this.formatDiagnosticResults(result));
        return headerLines.join('\n');
    }
}
exports.ContainerRegistryDiagnoseProvider = ContainerRegistryDiagnoseProvider;
