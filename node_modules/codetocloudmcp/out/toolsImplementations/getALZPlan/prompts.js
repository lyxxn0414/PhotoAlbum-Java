"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.populateGetALZPlanPrompts = populateGetALZPlanPrompts;
const toolType_1 = require("../shared/toolType");
function populateGetALZPlanPrompts(iacType, stateManager) {
    const iacRulesTool = stateManager.getToolName(toolType_1.ToolType.GetAzureResourceRules);
    const wafRulesTool = stateManager.getToolName(toolType_1.ToolType.GetWAFRules);
    const regionSkuTool = stateManager.getToolName(toolType_1.ToolType.GetRegionsForDeployment);
    return `
    { 
      Agent should fill in and polish the markdown template below to produce an IaC files generation plan for a customized Azure Landing Zone. Then save it to '.azure/landing_zone_plan.copilotmd' file. 
      
      DO:
        - Ask the user if they have any more requirements or preferences for the landing zone, update the plan accordingly if they do.
        - Once the user confirms, execute the steps defined in the plan file.

      DON'T:
        - Don't use azd !!!
        - Don't add more sections beyond those specified below.
    }

    # Customized Application Landing Zone Generation Plan

    ## Goal
    Based on the user's inputs and requirements, generate IaC files using **${iacType}** for a customized Azure application landing zone.

    ## User Requirements
    {
      A concise summary of the user's inputs, requirements and preferences for the landing zone.
    }

    ## Proposed Architecture
    {
      A **high level** Mermaid diagram illustrating the proposed Azure resource architecture to be generated, that meets the user's requirements.

      **Important guidelines:** 
      1. Make the diagram high level, concise and visually appealing.
      2. Use a **dark** background color for diagram components to improve visibility.
      3. Arrange elements neatly and minimize intersecting lines.
    }

    {
      - Briefly describe the key components and main highlights of the architecture using a table or list format.
      - If a reference architecture is provided, explain how the proposed architecture aligns with it, focusing on the primary service types.
    }

    ## Azure Resource List
    {
      A complete list of Azure resources to be generated. Use \`#appmod-get-azure-pricing\` to retrieve pricing information for each resource.

      | Resource Type | Resource Name | SKU | Est. Monthly Cost | Purpose |
      |---------------|---------------|-----|--------------------|---------|
      | [e.g., SQL Database] | [e.g., sqldb-myapp-prod] | [e.g., S1] | [e.g., $25/mo] | [Purpose] |

      > **Note**: The estimated costs shown above are based on Azure retail prices and serve only as a rough estimation. Actual costs may differ due to enterprise agreements, reservations, or other discounts. For consumption-based (pay-as-you-go) resources, costs depend on actual usage and cannot be accurately estimated upfront.
    }

    ## Execution Steps
    { You may refine the wording and formatting, but do not change any of the information provided. }

    1. Collect rules for IaC generation
      - Collect and understand all user provided policies, requirements and preferences for the landing zone.
      - Call the '${iacRulesTool}' tool to retrieve Azure resource rules. Set the **deploymentTool** parameter to **azcli**. **Do NOT use azd.**
      - Call the '${wafRulesTool}' tool to retrieve Azure Well-Architected Framework (WAF) rules for the specified resource types.
      - Call the '${regionSkuTool}' tool to determine supported regions and available SKUs for IaC.
    2. Generate the customized landing zone using **${iacType}**, including:
      - An infra/ folder containing modular IaC files, with each component or resource type defined in its own module file, along with a main entry file and a default parameter file.
      - A deployment script to allow users to initiate deployment.
      - A 'README.md' including the architecture and resource list, make the doc clear and concise.
      - A '.azure/compliance.md' file listing how the generated files adhere to the collected rules in step 1.
    3. Validate all generated files to ensure they are runnable and free of syntax errors:
      - Call 'get_errors' on all generated files (including **parameter file**) and iterate until all errors are resolved.
      - Ensure all generated files (including **parameter file**) use real OS-specific line breaks (LF on Linux/macOS, CRLF on Windows), not literal '\n'.
      - Remove all intermediate files created during validation.
    4. Ask for user confirmation and help deploy the infrastructure to Azure.
    

    ## Reference
    { You may refine this section, but do not remove any of the items listed here. }

    If you prefer to start from the standard existing landing zones, please refer to the resources below.

    [Platform Landing Zone](https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/landing-zone/#platform-landing-zone-vs-application-landing-zones)
    - [azl terraform accelerator](https://github.com/Azure/alz-terraform-accelerator/tree/main/templates/platform_landing_zone)
    - [alz-bicep-accelerator](https://github.com/Azure/alz-bicep-accelerator)

    [Application Landing Zone](https://learn.microsoft.com/en-us/azure/architecture/landing-zones/landing-zone-deploy#application-landing-zone-architectures)
    - [Azure App Service application landing zone accelerator](https://github.com/Azure/appservice-landing-zone-accelerator)
    - [Azure Container Apps landing zone accelerator](https://github.com/Azure/aca-landing-zone-accelerator)
    - [Azure Kubernetes Services (AKS) landing zone accelerator](https://github.com/Azure/AKS-Landing-Zone-Accelerator)
    - [Azure OpenAI landing zone accelerator](https://github.com/Azure/aoai-landing-zone-accelerator)
    - [Azure API Management landing zone accelerator](https://github.com/Azure/apim-landing-zone-accelerator)
    - [Azure Integration Services application landing zone accelerator](https://github.com/Azure/Integration-Services-Landing-Zone-Accelerator)
    - [Azure RedHat OpenShift application accelerator](https://github.com/Azure/ARO-Landing-Zone-Accelerator)
    `;
}
