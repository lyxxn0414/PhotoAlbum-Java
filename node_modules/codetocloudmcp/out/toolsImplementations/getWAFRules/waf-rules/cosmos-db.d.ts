export declare const CosmosDBRules = "\n# Cosmos DB - WAF Rules for IaC\n\n- Deploy across multiple regions for high availability\n- Configure zone redundancy in supported regions\n- Select appropriate consistency level (Strong, Bounded Staleness, Session, Consistent Prefix, Eventual)\n- Configure automatic failover for multi-region deployments\n- Use serverless or provisioned throughput mode based on usage patterns\n- Configure private endpoints for network isolation\n- Enable encryption at rest with customer-managed keys\n- Configure firewall rules and virtual network access\n- Enable diagnostic logging to Log Analytics workspace\n- Configure backup mode (Continuous or Periodic)\n- Use RBAC for access control instead of account keys where possible\n";
