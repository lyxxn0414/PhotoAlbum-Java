import { IAzureClient } from "../../toolsImplementations/shared/azureClient";
export interface AppLogRetrieverOptions {
    azureClient: IAzureClient;
    resourceGroupName: string;
    limit?: number;
}
export declare enum SupportedResourceType {
    ContainerApp = "Microsoft.App/containerApps",
    AppService = "Microsoft.Web/sites|app",
    FunctionApp = "Microsoft.Web/sites|functionapp"
}
/**
 * LLM-friendly enum with simple, easy-to-remember values.
 * Used in schemas exposed to LLMs, then converted to SupportedResourceType internally.
 */
export declare enum LLMSupportedResourceType {
    ContainerApp = "containerapp",
    AppService = "appservice",
    FunctionApp = "functionapp"
}
export declare function toSupportedResourceType(llmType: LLMSupportedResourceType): SupportedResourceType;
export interface ResourceForLogs {
    name: string;
    type: SupportedResourceType;
}
export interface LLMResourceForLogs {
    name: string;
    type: LLMSupportedResourceType;
}
export declare function toLLMResourceForLogsArray(llmResources: LLMResourceForLogs[]): ResourceForLogs[];
